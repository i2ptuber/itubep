"""
__version__.py — версия моста itubep-bridge.

Единственное место, где хранится текущая версия. Обновляется вручную
при подготовке релиза (тэг в git должен совпадать: v{VERSION}).
Используется updater.py для сравнения с манифестом обновлений и
может использоваться где угодно ещё (например, в User-Agent запросов
к сайту, в логах и т.п.).
"""

VERSION = "0.1.0"
